# Зависимости рантайма сервиса проверки документов.
# Версии зафиксированы по рабочему стенду (Python 3.12). Обновлять осознанно, с прогоном тестов.

# Ядро аудита: вызов модели, отчёт, конфиг
openai==2.32.0
openpyxl==3.1.5
pandas==2.2.3
numpy==1.26.4
pydantic==2.13.2
pydantic-settings==2.15.0
pyyaml==6.0.3

# Парсеры форматов документов
python-docx==1.2.0
python-pptx==1.0.2
PyMuPDF==1.27.2.2
Pillow==12.2.0
pdf2image==1.17.0
requests==2.33.1
tenacity==9.1.4

# Web API
fastapi==0.136.0
uvicorn[standard]==0.45.0
sse-starlette==3.3.4
python-multipart==0.0.26

# Локальный layout-детектор (GPU) — отдельно: requirements-gpu.txt. По умолчанию layout и OCR
# работают по HTTP на удалённом сервере, GPU-стек не нужен.

# Системные пакеты (ставятся отдельно, нужны для конвертации DOCX/PPTX → PDF → PNG):
#   apt-get install libreoffice-writer poppler-utils    # Linux
#   brew install --cask libreoffice && brew install poppler   # macOS
