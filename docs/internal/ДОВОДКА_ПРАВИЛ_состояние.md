# МосАнализ — доводка правил проверки: состояние и журнал

> Документ-ориентир для того, кто подключается к работе. Что за задача, что уже сделано,
> как продолжать. Дата последнего обновления: 2026-07-09 (3).

---

## 0. АКТУАЛЬНОЕ СОСТОЯНИЕ (2026-09-07) — читать первым

**Дуальный сервис генерации** документов — `~/projects/mos_generated`, см. там `СОСТОЯНИЕ_ГЕНЕРАЦИЯ.md`
(тот СОЗДАЁт те же типы, этот ПРОВЕРЯЕТ). 15 generic-типов генерации готовы.
Канонический маппинг типов проверка↔генерация↔каталог заказчика: `~/Downloads/МАППИНГ_типов_МосМониторинг.md` (у владельца на Маке).

### 🆕 РЕФАКТОРИНГ ПОД ПЕРЕДАЧУ (07.09) — по плану аудита готовности

Цель — чтобы другой разработчик развернул копию у себя и завёл свои типы документов.
Аудит: `~/Downloads/АУДИТ_готовность_к_передаче_mos_analiz.md` (у владельца). Логика проверки и
правила не менялись; регресс после каждого шага сходится с базовым срезом (вердикты по акту
1-в-1, отчёт 10×8, API, спецдвижки, сквозная сверка). Все шаги — отдельными коммитами на `dev`.

| Шаг | Что | Было → стало |
|---|---|---|
| A1 | мусор | 7 `.bak`, `.codex` в git, дерево 99 МБ → 0, удалён, 62 МБ |
| A2 | зависимости | 5 пакетов не указаны, версии `>=` → `requirements.txt` (==), `-gpu`, `-dev`; чистая установка проверена |
| A3 | тесты | 63 passed / 23 skipped / 3 failed → 124 / 12 / 0; +`test_registry.py`; фикстуры в `tests/data/` (не в git, архив `ПЕРЕДАЧА_фикстуры_тестов.zip`) |
| A4 | конфигурация | IP стенда в 42 строках, пароль в коде → 0; всё через `.env` (`config/llm.py`, `config/parsers.py` на pydantic-settings); из 25 config.json убран `llm_base_url`; без `AUDIT_*` сервер не стартует с понятной ошибкой; закрыт скрытый баг `LLM_CONFIG.model` |
| A5 | реестр | две копии в `main.py` и `server.py` → одна в `src/engines.py` |
| A6 | клиент модели | 5 мест создания `OpenAI(` → фабрика `make_llm_client` в `src/llm/client.py`; параметры вызовов не тронуты |
| A7 | контракты | имя модели `Qwen3.5` в 31 конфиге и коде → `Qwen3.6` из `LLM_MODEL`, поле `model` из config.json убрано; устаревшие числа и «Spark» в контрактах вычищены |
| A8 | докстринги | функции с докстрингом 76 % → 97 %, с полными аннотациями 78 % → 93 % (kpsc-парсер, валидаторы 0.3/0.4/3.1, сверка); тела сверены по AST |
| A9 | запуск | мёртвые docker-compose/Dockerfile/nginx/start_server (прод апреля) удалены → `scripts/` (restart_backend/frontend/all, healthcheck) с параметрами из `.env`; `~/wf_*.sh` — симлинки |
| A10 | документация | `README.md`, `docs/ADD_DOC_TYPE.md`, `docs/ARCHITECTURE.md` (+5 ADR), обновлён `CLAUDE.md` |

Впереди: A11 Docker (весь стенд одной командой), этап B — то же для `mos_generated`, C — приёмка.

### РАБОТЫ 26–31.08 (отчётность, доступы, материалы)

Кода эти работы не касались — это обвязка вокруг проектов.

**Отчёт руководству по статусу ИИ-ассистента.** Запросили фактуру за период: сколько документов
обработано, каким качеством, сколько времени. Собрано из журналов систем, каждая цифра проверяема.
Ключевое: измеримый период работы системы — **февраль–август 2026**, а не с ноября 2025, как
спрашивали: первый коммит продукта 09.02.2026, самые ранние сохранённые прогоны — 19.02.2026
(на Spark), артефактов раньше нет нигде. Всего прогонов **688** (515 на текущем стенде +
173 на Spark). Файлы: `~/projects/ФАКТУРА_ИИ-ассистент_отчёт.md` (сырьё с источниками),
`~/projects/ОТЧЁТ_блок1_статус_функционала.md`, `~/Downloads/ЦИФРЫ_ИИ-ассистент.txt` и `.xlsx`
(итоговые таблицы для руководителя).

⚠ Две оговорки, которые important при защите: почти все 688 прогонов — **наши, разработческие**,
работы конечных пользователей в них практически нет; метрики качества (F1 0,94) сняты на прежней
версии модели, в июле её сменили — требуется пересъёмка.

**Доступ к Битрикс24 по API.** Портал `b24.efficiency.mos.ru`, входящий вебхук создан владельцем,
лежит в `~/.bitrix_webhook` на Маке (в переписку не выводить). Читаются задачи (`tasks.task.list`,
`tasks.task.get`) и комментарии. Проверено: проект ведётся в группе 70 «ЦИФРА (Разработка
ИИ-ассистентов)», спринты — «Проверка документов» (27 подзадач) и «Создание документов ППТ»
(25 подзадач). Требований к карточкам 0.2/2.4 в задачах нет — описания пустые.

**Скриншоты для презентации** (коллеге, по статусу ассистента): `~/Downloads/СКРИНЫ_презентация/` —
панель правил, что получает модель на вход, устройство правила, отчёт с замечаниями, форма
генерации, автозаполнение по организации, перенос значений между типами, готовый документ.
Все с красными аннотациями. Excel-отчёт для кадра с выгрузкой снимает владелец сам.

---

### 🆕 ЧТО СДЕЛАНО С 24.07 (записи от 2026-08-24)

Три работы подряд: сквозная сверка комплекта, объединение двух фронтов в один домен, закрытие
рассинхронов реестров. Всё живое на стенде, **ничего не закоммичено** (список внизу §0).

#### 1. Новый тип «сквозная сверка 3 документов» — `crosscheck_2_4_0_6_0_5` (31.07)
**Зачем:** заказчик на созвоне попросил проверять не документ поодиночке, а СВЯЗКУ 2.4 Карточка +
0.6 Протокол + 0.5 Приказ о тираже — они должны биться между собой (цели↔факт, ООО, направление показателей).
**Как сделано:** отдельный тип со своим движком (`engine: crosscheck_2_4_0_6_0_5`, раннер
`src/doc_type_validators/crosscheck.py`). R1 «комплектность» — Python-гейт по префиксам/ключевым словам имён файлов;
дальше парсинг в размеченный контекст (Карточка = целевой лист целиком таблицей, остальные два = весь текст)
→ ОДИН вызов Qwen с правилами → JSON-вердикты → Excel-отчёт. Приём трёх файлов — эндпоинт `POST /api/audit/cross`
(сохраняет в папку сессии и отдаёт раннеру ПАПКУ вместо файла); в UI — 3 именованных слота, кнопка запуска
неактивна пока не выбраны все три.
**Почему LLM, а не Python-сверка:** первая версия была детерминированной, но владелец потребовал устойчивости
к вариациям формата документов — переписано на LLM.
**Проверено:** happy-комплект → 0 нарушений, unhappy → 3 с обоснованием, негатив по комплектности, API, UI.
**Долг:** фикстуры полусинтетические (данные правились в болванках) — для доводки нужен РЕАЛЬНЫЙ связанный
комплект 2.4+0.6+0.5 одного предприятия.

#### 2. Объединение аудита и генерации в один домен (24.08)
**Зачем:** было два фронта на двух доменах (`mosaudit` и `mosgen`), заказчику неудобно.
**Как сделано:** хост — React `ui-demo` аудита, слева сайдбар «Аудит» / «Генерация». Генерация ходит на `:8090`
через прокси `/gen` с rewrite (`vite.config.js`, блоки `server` И `preview`). **Префикс обязателен** — оба
бэкенда отдают `/api/types/*`, без него коллизия. Форма генерации портирована в React 1:1
(`ui-demo/src/ScreenGeneration.jsx`): список типов, схема полей, валидация обязательных по схеме типа,
нативное скачивание через скрытый iframe, автозаполнение по ООО.
**Важная деталь каркаса:** экраны аудита НЕ размонтируются при уходе на генерацию (обёрнуты в `hidden`) —
иначе рвался бы SSE и терялся текущий прогон. Проверено: уход на генерацию во время аудита, прогон досчитался в фоне.
**Домен:** остаётся `mosaudit.ru.tuna.am`; `mosgen.ru.tuna.am` живёт fallback'ом до приёмки владельцем.
**Побочно:** генерация теперь за логином аудита (раньше была открыта) — зафиксировано как решение.
**Известный дефект (в беклоге, владелец отложил):** в консоли при завершении аудита
`SyntaxError: "undefined" is not valid JSON` — слушатель на SSE-событие `error` ловит ещё и ВСТРОЕННОЕ
DOM-событие EventSource (у него нет `.data`), которое прилетает при штатном закрытии стрима после `complete`.
На работу не влияет. Фикс: `if (typeof e.data !== 'string') return;` в `subscribeToProgress` (`ui-demo/src/api.js`).

#### 3. ⚠ ЗАКРЫТЫ РАССИНХРОНЫ РЕЕСТРОВ СПЕЦДВИЖКОВ (24.08) — 8 типов не работали через UI
**Как всплыло:** коллега (Саша) пожаловался на 3.1: PDF роняет аудит `ValueError: не сконфигурирован парсер
для '.pdf'`, а родной xlsx интерфейс не принимает вовсе.
**Корень:** `SPECIAL_ENGINE_RUNNERS` объявлен В ДВУХ МЕСТАХ — `main.py` (CLI) и `src/api/server.py` (веб).
При выносе FastAPI (`bd89908`) перенесли не все движки, и с тех пор часть типов работала только из командной строки.

Четыре отдельных дефекта:

| Дефект | Кого задевал | Исправление |
|---|---|---|
| в веб-реестре нет `list_prisutstviya`, `forma_0_3`, `forma_0_4` | 3.1, 3.4, 0.3, 0.4 | реестры приведены к одному составу (9 = 9) |
| ключ `kartochka_proekta` не совпадал с конфигами `kartochka_proekta_0_2` / `_2_4` | 0.2, 2.4 | ключи исправлены, мёртвая запись убрана |
| `_run_special_engine` принимал `doc_type`, но не клал его в `args` | 0.2/2.4 падали; **3.4 МОЛЧА проверялся правилами 3.1** | `doc_type` передаётся |
| `_get_allowed_extensions` отдавал зашитую константу вместо форматов типа | 2.5/2.8 не брали `.pptx`; `.doc/.odt/.rtf` принимались без парсера и роняли аудит в середине | список выводится из `parser_by_ext` конфига |

Последний — ключевой: гейт форматов больше не отдельный список, который можно забыть обновить. Сверка по всем
31 типу: 0 расхождений между «что тип умеет парсить» и «что пускает интерфейс».

**Проверено на реальных файлах:** 3.1 — 13 правил/5 замечаний (файл Саши, через UI), 3.4 — 13/12, 0.3 — 14/9,
0.4 — 15/8, 0.2 — **6** правил/1, 2.4 — **11** правил/7 (разные наборы = `doc_type` доходит), 2.8 принял pptx,
`.doc` отсекается сразу с честным списком, регресс docx (0.6) — 16/7/44.0с (как до правок).

**⚠ ПРАВИЛО НА БУДУЩЕЕ: добавляя спецдвижок — прописать в ОБА реестра** (`main.py` и `src/api/server.py`),
иначе тип будет работать из CLI и молчать в интерфейсе.

#### 4. Снят лимит 10 участников в листах присутствия 3.1 / 3.4 (24.08)
**Зачем:** парсер читал жёстко строки 6–15 (стандартный бланк на 10 человек). В реальном документе Саши
участников **17** — семеро не проверялись вообще, при этом отчёт выглядел как полная проверка. Тихий дефект:
снаружи не отличить от нормального результата.
**Почему нельзя было просто «читать до конца листа»:** разведка трёх реальных бланков показала разную природу.
У «Пром-Энерго» ниже таблицы, через одну пустую строку, лежит служебный блок-памятка («ПРОВЕРЬ: …»,
«Фамилия, имя и отчество заполнена…») — наивное чтение до `max_row` затащило бы его в участники и дало ложные нарушения.
**Как сделано:** таблица читается подряд от строки 6, конец — **первая полностью пустая строка** (по колонкам A..I).
Это и есть природа бланка: таблица сплошная, ниже неё либо ничего, либо примечания, отделённые пустой строкой.
Критерий «строка заполнена» (ФИО ИЛИ организация ИЛИ e-mail) не менялся. Правка одна, в
`src/doc_type_parsers/list_prisutstviya.py`; бэкап `/tmp/parser_lp.bak.py`.
**Проверено на трёх разных бланках:** Еврокомфорт 8 → 8 (не изменилось), Пром-Энерго 4 → 4 (памятка отсечена,
стоп на строке 16), Джифлорс (файл Саши) 10 → **17**. В UI: 13 правил / 5 замечаний / 2.3 с, и в замечаниях
по e-mail и телефону появились **строки 17, 19, 22** — люди, которых раньше не смотрели.
**Осталось (косметика, не делали):** тексты правил в валидаторе всё ещё пишут диапазон «B6:B15», «G6:G15» —
после снятия лимита это неточно, лучше «столбец B таблицы участников».

**Попутно выяснено (не чинили, вне scope):**
- PDF для 3.1/3.4 не поддерживается в принципе (валидатор только openpyxl), а подписи есть ТОЛЬКО в скане;
- все 7 спецдвижков читают xlsx, включая `plan_grafik` (прежняя пометка «plan_grafik — PDF» была ошибочной).

### 🆕 ТИП 1.6 `spravka_ee` ЗАВЕДЁН (2026-07-24, generic-LLM, не доведён)
«1.6 Справка о расчёте экономического эффекта (факт)». Создан `doc_configs/spravka_ee/`: `config.json` (без `engine` = generic) + `sections.json` (9 секций) + `rules_multi.json` (10 правил из ТЗ эксперта) + `template/template.pdf` (скан Профсервиса). methodology-слой НЕ делали (опционален, вредит скору). Тип авто-подхватился сканом `doc_configs/` (реестра нет; рестарт не обязателен, но сделан). Живой прогон скана 54с — все 10 правил отработали, OCR (PaddleOCR-VL `:11438` + layout Heron-101 `:11439`) прочитал всю таблицу. На «верном» эталоне 5 флагов — доводочные, НЕ дефекты дока: #1 filename (артефакт: назвали `template.pdf`), #2 заголовок (OCR исказил длинную строку), #6×3 (by-design пустые «после» у годовых значений), **#9×4 подписи — OCR не распознал блок подписи+печать на скане (системный риск FP на всех скан-справках)**, #10 логика (поле `reasoning` разболталось на трудном правиле). Thinking подтверждён OFF (`multi_rule.py:565` шлёт `enable_thinking:False`, `<think>`=0 в ответе — «простыня» #10 это штатное `reasoning`→«Обоснование», не утечка). Осталось: доводка + BAD-образцы (вне scope). Артефакты эксперта: `~/Downloads/archive26-07-24/` (у владельца на Маке). ТИП 1.4 (xlsm-модель ЭЭ) — на созвоне, ТЗ эксперт не дал, кандидат special-runner по xlsx.

### ⚠ СМЕНА LLM-МОДЕЛИ (2026-07-24) — бенчмарк ниже снят на СТАРОЙ модели
Метрики F1 0.95 (таблица) получены на **`Qwen3.5-35B-A3B` (vLLM)** на `172.16.10.35:11437`.
Влад пересобрал хост: теперь `:11437` = **`Qwen3.6-35B-A3B` (llama.cpp/GGUF)**, `qwen-fast` на `:11436`, OCR `:11438`.
llama.cpp игнорирует имя модели в запросе → сервис работает без правок кода, но модель ДРУГАЯ → вердикты слегка иные.
**ПЕРЕД новой доводкой перепрогнать бенчмарк на 3.6.** Хост Влада, SSH туда нет; падение МосАудита =
`curl 172.16.10.35:11437/v1/models` → refused → писать Владу.

### Замер качества по бенчмарку: БЫЛО → СТАЛО (base-слой, 12 доведённых типов; ⚠ на Qwen3.5)
Оба замера — реальные прогоны бенчмарка. Baseline: `~/projects/_benchmark/metrics_baseline_2306.json`
(23 июня, ДО доводки и движок-фиксов). После: `~/projects/_benchmark/metrics_after.json` (14 июля).

| Метрика | БЫЛО | СТАЛО |
|---|---|---|
| **F1** | 0.69 | **0.95** |
| **Recall** (ловим брак) | 0.59 | **0.91** |
| **Precision** (нет ложных на эталонах) | 0.84 | **1.00** |
| Пропущено ошибок (fn) | 18 | 4 (некатчабельные/mismark) |
| Ложные срабатывания (fp) | 5 | 0 |

Как повторить замер (свежий прогон + скор):
```
cd ~/projects/mos_analiz_refactor && screen -dmS bench bash -c \
 ".venv/bin/python ~/projects/_benchmark/run_batch.py --only <тип1,тип2,...> --scope score > /tmp/bench_run.log 2>&1; echo DONE_BENCH >> /tmp/bench_run.log"
# ~2 мин/док. По завершении:
.venv/bin/python ~/projects/_benchmark/score_all.py ~/projects/_benchmark/results ~/projects/_benchmark/dataset.json ~/projects/_benchmark/metrics_after.json
```

### ⚠ КРИТИЧНО: живой :8081 надо перезапускать после правок
Python НЕ подхватывает изменения правил/кода на лету (uvicorn без `--reload`). После любой доводки/правки
кода живой `:8081` ОБЯЗАТЕЛЬНО перезапустить, иначе демонстрируется старый код. Бенчмарк-прогонов НЕ касается
(они стартуют `main.py` заново). В этой сессии :8081 перезапущен 14.07 на текущий код (до этого висел от 8 июля).
```
screen -S backend8081 -X quit; sleep 2
cd ~/projects/mos_analiz_refactor && screen -dmS backend8081 bash -c \
  "cd ~/projects/mos_analiz_refactor && exec .venv/bin/python -m uvicorn main:app --host 127.0.0.1 --port 8081 >> /tmp/backend8081.log 2>&1"
curl -s http://127.0.0.1:8081/api/health   # 200
```
Убивать процессы — bracket-трюком `[u]vicorn`/`[m]ain.py` (иначе pkill убьёт свою ssh-сессию → dev=255; лечить `dev -K`).

### UI + ФИЧА КАСТОМ-ПРАВИЛ — ЗАКОММИЧЕНО И ЗАПУШЕНО (2026-07-16)
Коммиты `fe730e6` (feat) + `ef13c73` (docs), ветка `refactor/repo-cleanup`, **запушена в origin** (= локальный
прод-репо `/home/nemovm/projects/mos_analiz`; внешнего github нет). Вошло:
- Фиксы UI: анимация пайплайна (маппинг под реальные SSE-события `checking_rules`; шаг «Загрузка шаблона» авто-готов;
  убрана ложная красная вспышка — флаг `finished` в `api.js`), сессия (проба куки на монтировании `App.jsx` + кука 30 дн
  в `server.py`), раскладка `ScreenUpload` в 2 колонки, сортировка типов по коду, загрузка PDF.
- **Пользовательские кастом-правила** (только generic-LLM типы): overlay `doc_configs/<тип>/rules_custom.json` —
  база `rules_multi.json` НЕизменна, движок мёрджит base+custom в `load_multi_rule_config` **на каждый аудит (live без
  рестарта)**. Эндпоинты `GET/POST/PUT/DELETE /api/types/{t}/rules` + `/draft` (спец-модель Qwen переписывает сырьё
  эксперта в `check`; секции — мультивыбор из `sections.json`). Индексы кастома ≥200, база защищена. UI Apple-минимализм:
  список база(view-only)/кастом(правка/удаление), drill-in редактор с предпросмотром «Как поймёт модель». ТЗ:
  `~/Downloads/ТЗ_mosaudit_кастом_правила.md`, `~/Downloads/ТЗ_mosaudit_фронт_фиксы.md`.

### Тултип секций (2026-07-24, НЕ закоммичено)
Чипы «Где проверять» → кастомный тултип 0.5с с описанием + границами блока (`start`/`end` из `sections.json`;
эндпоинт отдаёт их в `sections[]`; компонент `SectionChip`). Страницу/точную позицию НЕ показываем (нет данных).

### Осталось (generic)
`polozhenie_po`, `polozhenie_comp_ppu`, `prikaz_formirovanie_po`, `otchet_rezultatov` — ещё не доведены в проверке.
Плюс special-раннеры (kpsc, kartochka, plan_grafik, forma_0_3/0_4, list_prisutstviya, drivers).
**🔜 Новое (след. сессия):** завести типы **1.4 / 1.6** (Расчёт планового/фактического эконом. эффекта) — в каталоге
есть, типа в проверке НЕТ; эксперты пришлют артефакты → разведка формата (xlsx→special по образцу `drivers` / docx→generic).

### Незакоммичено сейчас (ветка `refactor/repo-cleanup`, на 2026-08-24)
Кампания (12 типов + 2 движок-фикса) и UI-блок с кастом-правилами — **ЗАКОММИЧЕНЫ И ЗАПУШЕНЫ** (16.07).
Коммиты 31.07 (НЕ пушены): `aca61d3` тип 1.6 `spravka_ee`, `d286b9c` тултип секций, `6a565de` демо-правило ИНН.

**НЕ закоммичено (работы 31.07 + 24.08):**
```
M  main.py                       реестр спецдвижков (+ crosscheck)
M  src/doc_type_parsers/list_prisutstviya.py   снят лимит 10 участников
M  src/api/server.py             эндпоинт /api/audit/cross, реестр, передача doc_type, гейт форматов
M  ui-demo/vite.config.js        прокси /gen -> :8090 с rewrite (server + preview)
M  ui-demo/src/App.jsx           каркас с сайдбаром, режимы audit/generation
M  ui-demo/src/api.js            функции генерации через /gen/api
M  ui-demo/src/ScreenUpload.jsx  3 слота сквозной сверки
?? ui-demo/src/Sidebar.jsx       левое меню
?? ui-demo/src/ScreenGeneration.jsx  порт формы генерации
?? doc_configs/crosscheck_2_4_0_6_0_5/
?? src/doc_type_validators/crosscheck.py
?? docs/internal/ПЛАН_объединение_UI.md
```
Бэкапы правок этой сессии: `/tmp/server.py.bak2`, `/tmp/server.py.bak3`, `/tmp/main.py.bak2`,
`/tmp/vite.config.js.bak`, `/tmp/App.jsx.bak`, `/tmp/api.js.bak`.
Мусор от теста кастом-правил: `doc_configs/polozhenie_ppu/rules_custom.json` = `{"rules": []}` (пустой, безвредный).

---


## 1. О чём вообще задача

**Чек-сервис «МосАудит»** проверяет типовые документы проекта по производительности труда
(приказы, положения, акты, протоколы, чек-листы, формы) на соответствие методике и находит ошибки.

Есть **бенчмарк** (`~/projects/_benchmark/`): эксперты разметили реальные документы —
на каждый тип обычно **4 «плохих» (BAD)** дока с намеченными ошибками + **2 «эталонных» (OK)** без ошибок.

**Задача кампании:** довести правила КАЖДОГО типа до «идеальной метрики»:
- все BAD ловят свои **покрываемые** размеченные ошибки (на уровне правил);
- оба OK-эталона **чисты** (0 ложных срабатываний базового слоя);
- часть ошибок эксперта — принципиально некатчабельны (корректность значения без эталона)
  или ошибочно размечены (mismark) — их **осознанно исключаем**, не выдумывая оверфит-правил.

Делаем **по одному типу за итерацию**, не перепрыгивая.

---

## 2. Как устроен чек-сервис (минимум для понимания)

- Репозиторий: `~/projects/mos_analiz_refactor` (на dev-стенде `ms-llm-dev`, доступ — инструмент `dev`).
- Правила по каждому типу: `doc_configs/<тип>/`:
  - `rules_multi.json` — **base**-правила (структура, реквизиты) — по ним считается основная метрика;
  - `rules_methodology.json` — **meth**-правила (соответствие методичке, строже, справочные; часто вне экспертного скоринга);
  - `sections.json` — карта секций документа (модель получает её как подсказку «где что искать»);
  - `config.json` — тип движка (`generic` = LLM-проверка; иначе — special-раннер).
- Движок посылает модели **весь текст документа** + карту секций + список правил.
  По каждому правилу модель отвечает `ok` / `fail` + обоснование. В результат попадают только `fail`.
- Правило смотрит в `target_section(s)` — это **подсказка**, а не фильтр (модель видит весь док).

### Как гонять и смотреть результат
```
# прогон одного типа через бенчмарк (свежий процесс — всегда актуальный код+правила):
rm -rf ~/projects/_benchmark/results/<тип>
cd ~/projects/mos_analiz_refactor && screen -dmS run bash -c \
  ".venv/bin/python ~/projects/_benchmark/run_batch.py --only <тип> --scope score > /tmp/run.log 2>&1"
# вердикт по КАЖДОМУ правилу (в т.ч. ok) — в сессии:
#   results/<тип>/<сессия>/multi_rule_response_parsed.json      (base)
#   results/<тип>/<сессия>/multi_rule_methodology_response_parsed.json (meth)
# final_results.json — ТОЛЬКО нарушения (для агрегата, не для «что ok»).
```

---

## 3. Что уже сделано (журнал)

Всё на ветке `refactor/repo-cleanup`, коммиты **НЕ запушены** (ждём слова владельца).
Автор коммитов: `Maskedxxx <aangers07@gmail.com>`.

### Доведено 12 типов (каждый прошёл гейт ревьюера #2 «годно»)
| Тип | Коммит | Итог |
|---|---|---|
| polozhenie_ppu | `316f6a4` | rule-level 100% |
| akt_nachala | `70a692a` | 4/4 BAD, эталоны чисты |
| cheklist_eu | `07531ae` | +правило подписей; 4/4, эталоны чисты |
| prikaz_tirazh | `ec85363` | 4/4 (приложения, подписи); эталоны чисты |
| prikaz_ic | `a8c318a` | оба эталона чисты; +секция «приложения» в разметку |
| protokol_vypolneniya | `f9f84e5` | 4/4; +правило консистентности дат; город=Москва |
| prikaz_ic_potoka | `bed6ff2` | 4/4; +правило дат (начало ≥ приказа); 3/4 ошибок НАЯДА = mismark |
| prikaz_pa | `2778297` | 3/4; +правило листов-приложений, преамбула=fail; ФорСИ = mismark |
| prikaz_vyhod | `745847a` | 3/3 флагуются; массовая meth-сатурация снята, +наименование, заголовок=fail; НАЯДА = mismark |
| prikaz_otvetstvennyh | `2898891` | уже ловил 4/4 base-правилами; +правило 12 (преамбула, отсутствие=fail) закрыло Еврокомфорт; БФС = mismark |
| prikaz_ppu | `aabcea5`+`a712861` | base: rule5 город=Москва (хардкод, одобрен), +правило 9 преамбула, +правило 10 ФИО ответственного; +ослаблена meth-сатурация §3.18 (103-110) → эталоны 0 нарушений base+meth; ГофроМир = mismark юрформы |
| prikaz_comp_ppu | `7e0e598` | base: +правило 9 лист ознакомления (отсутствие=fail) ловит Слон+Горячие; Гофромир→должность(7); +ослаблена meth 102-110 §3.18 → эталоны 0 base+meth; Гофромир/Горячие = mismark; город=Москва не применялся |

**Реестр mismark-разметки:** `docs/internal/MISMARK_реестр.md` (в корне репо) — кейсы, где разметка эксперта противоречит файлу (ФорСИ, НАЯДА) → на перепроверку версий. Проверять каждую «дыру» чтением файла (§3.13, §3.20 скила).

### Движок-фиксы (влияют на ВЕСЬ сервис)
| Коммит | Что |
|---|---|
| `2d4f211` | **Ретрай при «каше».** На крупных доках модель иногда отдаёт 1 объект вместо массива вердиктов → весь base-слой молча обнулялся (BAD ложно проходил). Ретрай: до N попыток со сменой seed, берётся лучшая по числу годных вердиктов. Код: `src/llm/multi_rule.py` → `run_multi_rule_audit`. |
| `3d55b23` | max_attempts ретрая **3 → 5** (3 не всегда хватает на больших документах). |

---

## 4. Методика доводки одного типа

Подробно — в скиле `mosanaliz-tune-playbook` (на стенде `~/.claude/skills/mosanaliz-tune-playbook/SKILL.md`).
Кратко цикл:

1. **Фаза 0** — собрать корпус ≥10 доков типа из всех источников в `~/projects/_corpus/<тип>/` (бенчмарк + прод `~/projects/mos_analiz` + логи юзерских загрузок).
2. **Фаза A** — аудит легитимности всех правил на корпусе (проверяет реально присутствующее? смотрит в ту секцию? не требует отсутствующего?).
3. **Агент-ревьюер #1** — независимо перепроверяет легитимность и катчабельность (сам читает доки/правила).
4. **Тюнинг (Шаги 1-10):** прогон эталонов → маппинг ошибок эксперта на правила → секция → root-cause → хирургичный фикс с бэкапом → перепрогон → сверка → регресс.
5. **Агент-ревьюер #2** — гейт качества (7 проверок): фикс ловит цель, нет оверфита/размывания, эталон не сломан, полнота, чтение верное.
6. **Коммит** — с апрувом владельца.

### Пограничные кейсы — как решали (важно, §3.12-3.17 скила)
- «Значение неверно» (дата/год) — часто это **консистентность полей**, а не абсолютная корректность → формализуется (дата начала ≥ даты приказа; дата шапки = дата конца периода). Ловит BAD, чисто на эталоне.
- «Нет блока/таблицы X» — **сначала посмотреть docx-таблицы / глазами**: часто блок ПО ФАКТУ есть (mismark эксперта) или у эталонов ровно так же (не обязателен). Правило под mismark не добавлять.
- Правило смотрит не в размеченную секцию → **добавить секцию в `sections.json` / нацелить `target_sections`** (не размывать текст правила).
- Правило требует того, чего нет почти ни в одном доке корпуса → **удалить правило**.
- **Город=Москва** — согласованный проектный хардкод: «если указан — = Москва; отсутствие — норма». Риск для регионов зафиксирован.
- **Типы-близнецы** (prikaz_ic ↔ prikaz_ic_potoka) — переиспользовать отлаженные формулировки, но перепроверять на СВОИХ эталонах.

---

## 5. Как продолжать

Владелец запускает следующий тип командой `/goal <текст цели>` (автономный прогон, ревьюер вместо апрувов).

**Следующий рекомендованный тип:** `polozhenie_po` или `prikaz_formirovanie_po`.

**Очередь (docx/generic, осталось):** prikaz_formirovanie_po, polozhenie_po, polozhenie_comp_ppu, otchet_rezultatov. (prikaz_pa, prikaz_vyhod, prikaz_otvetstvennyh, prikaz_ppu, prikaz_comp_ppu — доведены.)

**Особый формат (свои раннеры, НЕ generic — разбирать отдельно):** forma_0_3/0_4, kartochka_proekta_0_2/2_4, kpsc, plan_grafik, list_prisutstviya_modul_1/2, drivers.

*(Метрики бенчмарка от 06-23 — до фикса движка; часть прежних пропусков могла уже починиться. Точное состояние — на свежем прогоне в Фазе 0.)*

---

## 6. Инфраструктура (стенд)

**С 24.08 сервисы объединены под одним доменом.** `https://mosaudit.ru.tuna.am` — единый вход,
слева переключатель «Аудит» / «Генерация». Генерация ходит на свой бэкенд через прокси `/gen`
с rewrite (префикс обязателен: оба бэкенда отдают `/api/types/*`, без него коллизия).
`mosgen.ru.tuna.am` оставлен fallback'ом до приёмки, гасить по слову владельца.

| Что | Порт | Репо / запуск | Ссылка |
|---|---|---|---|
| **Проверка** | :8081 | `mos_analiz_refactor`, `scripts/restart_backend.sh`, screen `backend` | через `mosaudit.ru.tuna.am` (guest / пароль в `.env` стенда) |
| **Объединённый фронт** | :5174 | `scripts/restart_frontend.sh`, screen `frontend`; vite preview, прокси `/api` → :8081 и `/gen` → :8090 | `https://mosaudit.ru.tuna.am` |
| **Генерация** | :8090 | `mos_generated`, screen `mosgen` | `https://mosgen.ru.tuna.am` (fallback) |
| Старый прод | :8080 | `mos_analiz` — заморожен, правки кампании его НЕ трогают | — |

**ВАЖНО:** после правок кода/правил живой `:8081` надо **перезапустить** (Python не подхватывает
изменения на лету; uvicorn без `--reload`). Бенчмарк-прогоны это не касается — они запускают
`main.py` заново каждый раз. Перезапуск (с 07.09 — скриптами репозитория, логин/пароль и адреса
сервисов берутся из `.env` в корне):
```
bash scripts/restart_backend.sh     # бэкенд :8081, screen `backend`, лог /tmp/backend.log
bash scripts/restart_frontend.sh    # сборка + vite preview :5174, screen `frontend`
bash scripts/healthcheck.sh         # состояние бэкенда (LLM/OCR) и фронта
```

---

## 7. Открытые долги / решения владельца

Выверено 31.08.2026. Полный список задач по обоим сервисам — `~/projects/ТРЕКЕР_МосМониторинг.md`.

1. **Коммит и пуш** — по слову владельца. Оба репозитория на GitHub (ветка `dev`); коммиты
   рефакторинга 07.09 сделаны, **push ещё не делался**. ⚠ Репозитории публичные — переключить в приватные.
2. **Ревалидация бенчмарка на Qwen3.6** — метрики F1 0,95 сняты на прежней модели (3.5),
   в июле хост пересобрали. До пересъёмки цифрами качества оперировать осторожно.
3. **kpsc** — не синхронизирован рефакт↔прод (правило 7.6 + ERROR-валидаторы).
4. **Тип 1.6 `spravka_ee`** — заведён, не доведён: нужны BAD-образцы. На эталоне 5 флагов,
   главный риск — OCR не распознаёт блок подписи с печатью на скане.
5. **Сквозная сверка** — нужен реальный связанный комплект 2.4 + 0.6 + 0.5 одного предприятия;
   текущие фикстуры полусинтетические, владелец это отметил.
6. **Листы присутствия 3.1/3.4** — PDF-сканы не поддерживаются (валидатор только по xlsx),
   а подписи есть только в скане. Отдельная большая задача с OCR.
7. **Тип 1.4** (расчёт планового эффекта, xlsm-модель) — ТЗ эксперт не дал, кандидат в special-runner.
8. **Хардкод «г. Москва»** — при появлении легитимных региональных РЦК-доков даст ложные
   срабатывания; тогда параметризовать.
9. **Ошибка в консоли браузера** при завершении аудита (`"undefined" is not valid JSON`) —
   слушатель ловит встроенное DOM-событие `error` у EventSource. На работу не влияет,
   фикс в одну строку в `subscribeToProgress` (`ui-demo/src/api.js`), владелец отложил.

---

## 8. Мелочи, которые экономят время

- VPN до стенда нестабилен: перед командой — `dev -K >/dev/null 2>&1; sleep 2; dev '...'`. Длинные прогоны — только в `screen`, потом опрашивать.
- `pkill -f "<строка>"` НЕ должна матчить свою же ssh-команду (убьёт сессию). Гасить точечно.
- Правки правил — хирургично, python + base64, с бэкапом `.bak`, проверять `json.load`.
- macOS у оператора блокирует `~/Documents` — работать на стенде; локально скрипты из `~/Downloads`.
